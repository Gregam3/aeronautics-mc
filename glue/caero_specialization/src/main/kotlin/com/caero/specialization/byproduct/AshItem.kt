package com.caero.specialization.byproduct

import net.minecraft.world.item.Item

/**
 * v1: ash is produced by the refiner but has no consumer yet.
 * Future: forestry uses it as potash → fertilizer (PLAN.md §11).
 */
class AshItem(properties: Properties) : Item(properties)
